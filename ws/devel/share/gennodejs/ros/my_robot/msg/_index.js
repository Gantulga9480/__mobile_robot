
"use strict";

let board = require('./board.js');
let board_cmd = require('./board_cmd.js');
let next_move = require('./next_move.js');

module.exports = {
  board: board,
  board_cmd: board_cmd,
  next_move: next_move,
};
